import { initiateAuthentication as i } from "@coinbase/cdp-api-client";
import { getCoreAuthManager as t, getConfig as s } from "./index.web30.js";
async function g(n) {
  if (!await t().isSignedIn())
    throw new Error("User must be signed in to link a phone number");
  const e = await i(s().projectId, {
    phoneNumber: n,
    type: "sms"
  });
  return {
    flowId: e.flowId,
    message: "message" in e ? e.message : ""
  };
}
export {
  g as linkSms
};
