import { DisconnectedError as u, from as m } from "./index.native42.js";
import { http as p } from "viem";
import { createStore as g } from "zustand";
import { subscribeWithSelector as f, persist as S, createJSONStorage as I } from "./index.native43.js";
import { validateRequest as w, EIP1193ProviderError as C, STANDARD_ERROR_CODES as b } from "./index.native18.js";
import { EventEmitter as _ } from "./index.native44.js";
import { getPlatformStorage as A } from "./index.native6.js";
import "@coinbase/cdp-api-client";
import { handleGetCapabilities as v, handleGetCallsStatus as D, handleSendCalls as E, handleChainId as P, handleSignTypedData as R, handleEthSign as y, handleSwitchEthereumChain as T, handleDisconnect as q, handleSendTransaction as N, handlePersonalSign as O, handleRequestAccounts as G, handleAccounts as L } from "./index.native45.js";
import { onAuthStateChange as M } from "./index.native7.js";
import { baseSepolia as h } from "./index.native46.js";
function W(r, n, s) {
  n.subscribe(
    (e) => e.chainId,
    (e, t) => {
      e !== t && r.emit("chainChanged", e);
    }
  ), n.subscribe(
    (e) => e.user,
    (e, t) => {
      if (!(e?.userId === t?.userId && e?.evmAccounts?.length === t?.evmAccounts?.length)) {
        if (!e && t) {
          r.emit("disconnect", new u());
          return;
        }
        if (e && e.evmAccounts?.length && (!t || t.evmAccounts?.length === 0)) {
          const a = n.getState().chainId;
          r.emit("connect", { chainId: a });
          return;
        }
      }
    }
  );
  const { chainId: i, setChainId: o } = n.getState(), d = i || s.chains[0].id;
  return i || o(d), m({
    request: async (e) => {
      w(e, s);
      const t = e;
      switch (t.method) {
        case "eth_accounts":
          return L();
        case "eth_requestAccounts":
          return G(r, n);
        case "personal_sign":
          return O(t.params);
        case "eth_sendTransaction":
          return N(t.params, n, s.transports);
        case "wallet_disconnect":
          return q();
        case "wallet_switchEthereumChain":
          return T(t.params, n);
        case "eth_sign":
          return y(t.params);
        case "eth_signTypedData_v4":
          return R(t.params);
        case "eth_chainId":
          return P(n);
        case "wallet_sendCalls":
          return E(t.params, n);
        case "wallet_getCallsStatus":
          return D(t.params, n);
        case "wallet_getCapabilities":
          return v();
        // Catch-all for any unknown method
        default:
          throw new C(
            b.provider.unsupportedMethod,
            `Method ${t.method} not supported`
          );
      }
    },
    // Event emitter methods - only the ones required by the interface
    on: r.on.bind(r),
    removeListener: r.removeListener.bind(r)
  });
}
const l = {
  chains: [h],
  transports: {
    [h.id]: p()
  }
};
function V(r) {
  if (r?.chains.length === 0)
    throw new Error("At least one chain must be configured");
  const n = r?.chains ?? l.chains, s = r?.transports ?? l.transports;
  if (n.length === 0)
    throw new Error("No chains found in configuration");
  const i = new _(), o = A(), d = {
    getItem: (t) => o.getItem(t),
    setItem: (t, a) => o.setItem(t, a),
    removeItem: (t) => o.removeItem(t)
  }, c = g()(
    f(
      S(
        (t) => ({
          chainId: n[0].id,
          setChainId: (a) => t({ chainId: a }),
          chains: n,
          user: null,
          setUser: (a) => t({ user: a })
        }),
        {
          name: "cdp-provider-store",
          storage: I(() => d)
        }
      )
    )
  );
  return c.persist.rehydrate(), M((t) => {
    c.getState().setUser(t);
  }), {
    provider: W(i, c, {
      chains: n,
      transports: s
    })
  };
}
export {
  V as createCDPEmbeddedWallet
};
