import { initiateAuthentication as n } from "@coinbase/cdp-api-client";
import { getCoreAuthManager as t, getConfig as o } from "./index.web30.js";
async function g(i) {
  if (!await t().isSignedIn())
    throw new Error("User must be signed in to link an email");
  const e = await n(o().projectId, {
    email: i,
    type: "email"
  });
  return {
    flowId: e.flowId,
    message: "message" in e ? e.message : ""
  };
}
export {
  g as linkEmail
};
