import { InvalidAddressError as e } from "./index113.js";
import { isAddress as o } from "./index140.js";
function i(r, s) {
  if (!o(r, { strict: !1 }))
    throw new e({ address: r });
  if (!o(s, { strict: !1 }))
    throw new e({ address: s });
  return r.toLowerCase() === s.toLowerCase();
}
export {
  i as isAddressEqual
};
