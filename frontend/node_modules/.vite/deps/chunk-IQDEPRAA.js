import {
  B,
  F,
  H,
  I,
  O,
  R,
  S,
  U,
  V,
  _,
  b,
  c,
  k,
  l,
  v,
  w
} from "./chunk-Q4I2IT5S.js";

// node_modules/@coinbase/cdp-hooks/dist/esm/index260.js
function y(o, t, s, n) {
  if (typeof o.setBigUint64 == "function")
    return o.setBigUint64(t, s, n);
  const i = BigInt(32), h5 = BigInt(4294967295), e = Number(s >> i & h5), r2 = Number(s & h5), c3 = n ? 4 : 0, u = n ? 0 : 4;
  o.setUint32(t + c3, e, n), o.setUint32(t + u, r2, n);
}
function U2(o, t, s) {
  return o & t ^ ~o & s;
}
function _2(o, t, s) {
  return o & t ^ o & s ^ t & s;
}
var m = class extends F {
  constructor(t, s, n, i) {
    super(), this.finished = false, this.length = 0, this.pos = 0, this.destroyed = false, this.blockLen = t, this.outputLen = s, this.padOffset = n, this.isLE = i, this.buffer = new Uint8Array(t), this.view = _(this.buffer);
  }
  update(t) {
    B(this), t = b(t), c(t);
    const { view: s, buffer: n, blockLen: i } = this, h5 = t.length;
    for (let e = 0; e < h5; ) {
      const r2 = Math.min(i - this.pos, h5 - e);
      if (r2 === i) {
        const c3 = _(t);
        for (; i <= h5 - e; e += i)
          this.process(c3, e);
        continue;
      }
      n.set(t.subarray(e, e + r2), this.pos), this.pos += r2, e += r2, this.pos === i && (this.process(s, 0), this.pos = 0);
    }
    return this.length += t.length, this.roundClean(), this;
  }
  digestInto(t) {
    B(this), H(t, this), this.finished = true;
    const { buffer: s, view: n, blockLen: i, isLE: h5 } = this;
    let { pos: e } = this;
    s[e++] = 128, V(this.buffer.subarray(e)), this.padOffset > i - e && (this.process(n, 0), e = 0);
    for (let f = e; f < i; f++)
      s[f] = 0;
    y(n, i - 8, BigInt(this.length * 8), h5), this.process(n, 0);
    const r2 = _(t), c3 = this.outputLen;
    if (c3 % 4)
      throw new Error("_sha2: outputLen should be aligned to 32bit");
    const u = c3 / 4, p = this.get();
    if (u > p.length)
      throw new Error("_sha2: outputLen bigger than state");
    for (let f = 0; f < u; f++)
      r2.setUint32(4 * f, p[f], h5);
  }
  digest() {
    const { buffer: t, outputLen: s } = this;
    this.digestInto(t);
    const n = t.slice(0, s);
    return this.destroy(), n;
  }
  _cloneInto(t) {
    t || (t = new this.constructor()), t.set(...this.get());
    const { blockLen: s, buffer: n, length: i, finished: h5, destroyed: e, pos: r2 } = this;
    return t.destroyed = e, t.finished = h5, t.length = i, t.pos = r2, i % s && t.buffer.set(n), t;
  }
  clone() {
    return this._cloneInto();
  }
};
var B2 = Uint32Array.from([
  1779033703,
  3144134277,
  1013904242,
  2773480762,
  1359893119,
  2600822924,
  528734635,
  1541459225
]);

// node_modules/@coinbase/cdp-hooks/dist/esm/index241.js
var B3 = Uint32Array.from([
  1116352408,
  1899447441,
  3049323471,
  3921009573,
  961987163,
  1508970993,
  2453635748,
  2870763221,
  3624381080,
  310598401,
  607225278,
  1426881987,
  1925078388,
  2162078206,
  2614888103,
  3248222580,
  3835390401,
  4022224774,
  264347078,
  604807628,
  770255983,
  1249150122,
  1555081692,
  1996064986,
  2554220882,
  2821834349,
  2952996808,
  3210313671,
  3336571891,
  3584528711,
  113926993,
  338241895,
  666307205,
  773529912,
  1294757372,
  1396182291,
  1695183700,
  1986661051,
  2177026350,
  2456956037,
  2730485921,
  2820302411,
  3259730800,
  3345764771,
  3516065817,
  3600352804,
  4094571909,
  275423344,
  430227734,
  506948616,
  659060556,
  883997877,
  958139571,
  1322822218,
  1537002063,
  1747873779,
  1955562222,
  2024104815,
  2227730452,
  2361852424,
  2428436474,
  2756734187,
  3204031479,
  3329325298
]);
var h = new Uint32Array(64);
var E = class extends m {
  constructor(o = 32) {
    super(64, o, 8, false), this.A = B2[0] | 0, this.B = B2[1] | 0, this.C = B2[2] | 0, this.D = B2[3] | 0, this.E = B2[4] | 0, this.F = B2[5] | 0, this.G = B2[6] | 0, this.H = B2[7] | 0;
  }
  get() {
    const { A: o, B: r2, C: t, D: s, E: a, F: f, G: c3, H: e } = this;
    return [o, r2, t, s, a, f, c3, e];
  }
  // prettier-ignore
  set(o, r2, t, s, a, f, c3, e) {
    this.A = o | 0, this.B = r2 | 0, this.C = t | 0, this.D = s | 0, this.E = a | 0, this.F = f | 0, this.G = c3 | 0, this.H = e | 0;
  }
  process(o, r2) {
    for (let x5 = 0; x5 < 16; x5++, r2 += 4)
      h[x5] = o.getUint32(r2, false);
    for (let x5 = 16; x5 < 64; x5++) {
      const H4 = h[x5 - 15], d2 = h[x5 - 2], m3 = S(H4, 7) ^ S(H4, 18) ^ H4 >>> 3, l3 = S(d2, 17) ^ S(d2, 19) ^ d2 >>> 10;
      h[x5] = l3 + h[x5 - 7] + m3 + h[x5 - 16] | 0;
    }
    let { A: t, B: s, C: a, D: f, E: c3, F: e, G: n, H: A3 } = this;
    for (let x5 = 0; x5 < 64; x5++) {
      const H4 = S(c3, 6) ^ S(c3, 11) ^ S(c3, 25), d2 = A3 + H4 + U2(c3, e, n) + B3[x5] + h[x5] | 0, l3 = (S(t, 2) ^ S(t, 13) ^ S(t, 22)) + _2(t, s, a) | 0;
      A3 = n, n = e, e = c3, c3 = f + d2 | 0, f = a, a = s, s = t, t = d2 + l3 | 0;
    }
    t = t + this.A | 0, s = s + this.B | 0, a = a + this.C | 0, f = f + this.D | 0, c3 = c3 + this.E | 0, e = e + this.F | 0, n = n + this.G | 0, A3 = A3 + this.H | 0, this.set(t, s, a, f, c3, e, n, A3);
  }
  roundClean() {
    V(h);
  }
  destroy() {
    this.set(0, 0, 0, 0, 0, 0, 0, 0), V(this.buffer);
  }
};
var y2 = O(() => new E());

// node_modules/@coinbase/cdp-hooks/dist/esm/index264.js
var r = class extends F {
  constructor(t, i) {
    super(), this.finished = false, this.destroyed = false, U(t);
    const h5 = b(i);
    if (this.iHash = t.create(), typeof this.iHash.update != "function")
      throw new Error("Expected instance of class which extends utils.Hash");
    this.blockLen = this.iHash.blockLen, this.outputLen = this.iHash.outputLen;
    const o = this.blockLen, e = new Uint8Array(o);
    e.set(h5.length > o ? t.create().update(h5).digest() : h5);
    for (let s = 0; s < e.length; s++)
      e[s] ^= 54;
    this.iHash.update(e), this.oHash = t.create();
    for (let s = 0; s < e.length; s++)
      e[s] ^= 106;
    this.oHash.update(e), V(e);
  }
  update(t) {
    return B(this), this.iHash.update(t), this;
  }
  digestInto(t) {
    B(this), c(t, this.outputLen), this.finished = true, this.iHash.digestInto(t), this.oHash.update(t), this.oHash.digestInto(t), this.destroy();
  }
  digest() {
    const t = new Uint8Array(this.oHash.outputLen);
    return this.digestInto(t), t;
  }
  _cloneInto(t) {
    t || (t = Object.create(Object.getPrototypeOf(this), {}));
    const { oHash: i, iHash: h5, finished: o, destroyed: e, blockLen: s, outputLen: c3 } = this;
    return t = t, t.finished = o, t.destroyed = e, t.blockLen = s, t.outputLen = c3, t.oHash = i._cloneInto(t.oHash), t.iHash = h5._cloneInto(t.iHash), t;
  }
  clone() {
    return this._cloneInto();
  }
  destroy() {
    this.destroyed = true, this.oHash.destroy(), this.iHash.destroy();
  }
};
var y3 = (n, t, i) => new r(n, t).update(i).digest();
y3.create = (n, t) => new r(n, t);

// node_modules/@coinbase/cdp-hooks/dist/esm/index263.js
var y4 = BigInt(0);
var g = BigInt(1);
function k2(t, r2) {
  if (typeof r2 != "boolean")
    throw new Error(t + " boolean expected, got " + r2);
}
function j(t) {
  const r2 = t.toString(16);
  return r2.length & 1 ? "0" + r2 : r2;
}
function h2(t) {
  if (typeof t != "string")
    throw new Error("hex string expected, got " + typeof t);
  return t === "" ? y4 : BigInt("0x" + t);
}
function H2(t) {
  return h2(k(t));
}
function O2(t) {
  return c(t), h2(k(Uint8Array.from(t).reverse()));
}
function x(t, r2) {
  return I(t.toString(16).padStart(r2 * 2, "0"));
}
function S2(t, r2) {
  return x(t, r2).reverse();
}
function _3(t, r2, n) {
  let o;
  if (typeof r2 == "string")
    try {
      o = I(r2);
    } catch (e) {
      throw new Error(t + " must be hex string or Uint8Array, cause: " + e);
    }
  else if (w(r2))
    o = Uint8Array.from(r2);
  else
    throw new Error(t + " must be hex string or Uint8Array");
  const i = o.length;
  if (typeof n == "number" && i !== n)
    throw new Error(t + " of length " + n + " expected, got " + i);
  return o;
}
var w2 = (t) => typeof t == "bigint" && y4 <= t;
function T(t, r2, n) {
  return w2(t) && w2(r2) && w2(n) && r2 <= t && t < n;
}
function $(t, r2, n, o) {
  if (!T(r2, n, o))
    throw new Error("expected valid " + t + ": " + n + " <= n < " + o + ", got " + r2);
}
function M(t) {
  let r2;
  for (r2 = 0; t > y4; t >>= g, r2 += 1)
    ;
  return r2;
}
var N = (t) => (g << BigInt(t)) - g;
function R2(t, r2, n) {
  if (typeof t != "number" || t < 2)
    throw new Error("hashLen must be a number");
  if (typeof r2 != "number" || r2 < 2)
    throw new Error("qByteLen must be a number");
  if (typeof n != "function")
    throw new Error("hmacFn must be a function");
  const o = (u) => new Uint8Array(u), i = (u) => Uint8Array.of(u);
  let e = o(t), s = o(t), f = 0;
  const c3 = () => {
    e.fill(1), s.fill(0), f = 0;
  }, l3 = (...u) => n(s, e, ...u), m3 = (u = o(0)) => {
    s = l3(i(0), u), e = l3(), u.length !== 0 && (s = l3(i(1), u), e = l3());
  }, E3 = () => {
    if (f++ >= 1e3)
      throw new Error("drbg: tried 1000 values");
    let u = 0;
    const a = [];
    for (; u < r2; ) {
      e = l3();
      const b3 = e.slice();
      a.push(b3), u += e.length;
    }
    return R(...a);
  };
  return (u, a) => {
    c3(), m3(u);
    let b3;
    for (; !(b3 = a(E3())); )
      m3();
    return c3(), b3;
  };
}
function z(t, r2, n = {}) {
  if (!t || typeof t != "object")
    throw new Error("expected valid options object");
  function o(i, e, s) {
    const f = t[i];
    if (s && f === void 0)
      return;
    const c3 = typeof f;
    if (c3 !== e || f === null)
      throw new Error(`param "${i}" is invalid: expected ${e}, got ${c3}`);
  }
  Object.entries(r2).forEach(([i, e]) => o(i, e, false)), Object.entries(n).forEach(([i, e]) => o(i, e, true));
}
function D(t) {
  const r2 = /* @__PURE__ */ new WeakMap();
  return (n, ...o) => {
    const i = r2.get(n);
    if (i !== void 0)
      return i;
    const e = t(n, ...o);
    return r2.set(n, e), e;
  };
}

// node_modules/@coinbase/cdp-hooks/dist/esm/index243.js
var g2 = BigInt(0);
var d = BigInt(1);
var q = BigInt(2);
var V2 = BigInt(3);
var x2 = BigInt(4);
var T2 = BigInt(5);
var L = BigInt(8);
function h3(t, n) {
  const o = t % n;
  return o >= g2 ? o : n + o;
}
function O3(t, n, o) {
  let r2 = t;
  for (; n-- > g2; )
    r2 *= r2, r2 %= o;
  return r2;
}
function B4(t, n) {
  if (t === g2)
    throw new Error("invert: expected non-zero number");
  if (n <= g2)
    throw new Error("invert: expected positive modulus, got " + n);
  let o = h3(t, n), r2 = n, s = g2, f = d;
  for (; o !== g2; ) {
    const u = r2 / o, i = r2 % o, w3 = s - f * u;
    r2 = o, o = i, s = f, f = w3;
  }
  if (r2 !== d)
    throw new Error("invert: does not exist");
  return h3(s, n);
}
function M2(t, n) {
  const o = (t.ORDER + d) / x2, r2 = t.pow(n, o);
  if (!t.eql(t.sqr(r2), n))
    throw new Error("Cannot find square root");
  return r2;
}
function H3(t, n) {
  const o = (t.ORDER - T2) / L, r2 = t.mul(n, q), s = t.pow(r2, o), f = t.mul(n, s), c3 = t.mul(t.mul(f, q), s), u = t.mul(f, t.sub(c3, t.ONE));
  if (!t.eql(t.sqr(u), n))
    throw new Error("Cannot find square root");
  return u;
}
function K(t) {
  if (t < BigInt(3))
    throw new Error("sqrt is not defined for small field");
  let n = t - d, o = 0;
  for (; n % q === g2; )
    n /= q, o++;
  let r2 = q;
  const s = U3(t);
  for (; N2(s, r2) === 1; )
    if (r2++ > 1e3)
      throw new Error("Cannot find square root: probably non-prime P");
  if (o === 1)
    return M2;
  let f = s.pow(r2, n);
  const c3 = (n + d) / q;
  return function(i, w3) {
    if (i.is0(w3))
      return w3;
    if (N2(i, w3) !== 1)
      throw new Error("Cannot find square root");
    let e = o, l3 = i.mul(i.ONE, f), a = i.pow(w3, n), b3 = i.pow(w3, c3);
    for (; !i.eql(a, i.ONE); ) {
      if (i.is0(a))
        return i.ZERO;
      let m3 = 1, E3 = i.sqr(a);
      for (; !i.eql(E3, i.ONE); )
        if (m3++, E3 = i.sqr(E3), m3 === e)
          throw new Error("Cannot find square root");
      const C = d << BigInt(e - m3 - 1), v3 = i.pow(l3, C);
      e = m3, l3 = i.sqr(v3), a = i.mul(a, l3), b3 = i.mul(b3, v3);
    }
    return b3;
  };
}
function Q(t) {
  return t % x2 === V2 ? M2 : t % L === T2 ? H3 : K(t);
}
var Y = [
  "create",
  "isValid",
  "is0",
  "neg",
  "inv",
  "sqrt",
  "sqr",
  "eql",
  "add",
  "sub",
  "mul",
  "pow",
  "div",
  "addN",
  "subN",
  "mulN",
  "sqrN"
];
function P(t) {
  const n = {
    ORDER: "bigint",
    MASK: "bigint",
    BYTES: "number",
    BITS: "number"
  }, o = Y.reduce((r2, s) => (r2[s] = "function", r2), n);
  return z(t, o), t;
}
function k3(t, n, o) {
  if (o < g2)
    throw new Error("invalid exponent, negatives unsupported");
  if (o === g2)
    return t.ONE;
  if (o === d)
    return n;
  let r2 = t.ONE, s = n;
  for (; o > g2; )
    o & d && (r2 = t.mul(r2, s)), s = t.sqr(s), o >>= d;
  return r2;
}
function G(t, n, o = false) {
  const r2 = new Array(n.length).fill(o ? t.ZERO : void 0), s = n.reduce((c3, u, i) => t.is0(u) ? c3 : (r2[i] = c3, t.mul(c3, u)), t.ONE), f = t.inv(s);
  return n.reduceRight((c3, u, i) => t.is0(u) ? c3 : (r2[i] = t.mul(c3, r2[i]), t.mul(c3, u)), f), r2;
}
function N2(t, n) {
  const o = (t.ORDER - d) / q, r2 = t.pow(n, o), s = t.eql(r2, t.ONE), f = t.eql(r2, t.ZERO), c3 = t.eql(r2, t.neg(t.ONE));
  if (!s && !f && !c3)
    throw new Error("invalid Legendre symbol result");
  return s ? 1 : f ? 0 : -1;
}
function J(t, n) {
  n !== void 0 && l(n);
  const o = n !== void 0 ? n : t.toString(2).length, r2 = Math.ceil(o / 8);
  return { nBitLength: o, nByteLength: r2 };
}
function U3(t, n, o = false, r2 = {}) {
  if (t <= g2)
    throw new Error("invalid field: expected ORDER > 0, got " + t);
  let s, f;
  if (typeof n == "object" && n != null) {
    if (r2.sqrt || o)
      throw new Error("cannot specify opts in two arguments");
    const e = n;
    e.BITS && (s = e.BITS), e.sqrt && (f = e.sqrt), typeof e.isLE == "boolean" && (o = e.isLE);
  } else
    typeof n == "number" && (s = n), r2.sqrt && (f = r2.sqrt);
  const { nBitLength: c3, nByteLength: u } = J(t, s);
  if (u > 2048)
    throw new Error("invalid field: expected ORDER of <= 2048 bytes");
  let i;
  const w3 = Object.freeze({
    ORDER: t,
    isLE: o,
    BITS: c3,
    BYTES: u,
    MASK: N(c3),
    ZERO: g2,
    ONE: d,
    create: (e) => h3(e, t),
    isValid: (e) => {
      if (typeof e != "bigint")
        throw new Error("invalid field element: expected bigint, got " + typeof e);
      return g2 <= e && e < t;
    },
    is0: (e) => e === g2,
    // is valid and invertible
    isValidNot0: (e) => !w3.is0(e) && w3.isValid(e),
    isOdd: (e) => (e & d) === d,
    neg: (e) => h3(-e, t),
    eql: (e, l3) => e === l3,
    sqr: (e) => h3(e * e, t),
    add: (e, l3) => h3(e + l3, t),
    sub: (e, l3) => h3(e - l3, t),
    mul: (e, l3) => h3(e * l3, t),
    pow: (e, l3) => k3(w3, e, l3),
    div: (e, l3) => h3(e * B4(l3, t), t),
    // Same as above, but doesn't normalize
    sqrN: (e) => e * e,
    addN: (e, l3) => e + l3,
    subN: (e, l3) => e - l3,
    mulN: (e, l3) => e * l3,
    inv: (e) => B4(e, t),
    sqrt: f || ((e) => (i || (i = Q(t)), i(w3, e))),
    toBytes: (e) => o ? S2(e, u) : x(e, u),
    fromBytes: (e) => {
      if (e.length !== u)
        throw new Error("Field.fromBytes: expected " + u + " bytes, got " + e.length);
      return o ? O2(e) : H2(e);
    },
    // TODO: we don't need it here, move out to separate fn
    invertBatch: (e) => G(w3, e),
    // We can't move this out because Fp6, Fp12 implement it
    // and it's unclear what to return in there.
    cmov: (e, l3, a) => a ? l3 : e
  });
  return Object.freeze(w3);
}
function A(t) {
  if (typeof t != "bigint")
    throw new Error("field order must be bigint");
  const n = t.toString(2).length;
  return Math.ceil(n / 8);
}
function W(t) {
  const n = A(t);
  return n + Math.ceil(n / 2);
}
function D2(t, n, o = false) {
  const r2 = t.length, s = A(n), f = W(n);
  if (r2 < 16 || r2 < f || r2 > 1024)
    throw new Error("expected " + f + "-1024 bytes of input, got " + r2);
  const c3 = o ? O2(t) : H2(t), u = h3(c3, n - d) + d;
  return o ? S2(u, s) : x(u, s);
}

// node_modules/@coinbase/cdp-hooks/dist/esm/index265.js
var g3 = BigInt(0);
var h4 = BigInt(1);
function b2(t, r2) {
  const e = r2.negate();
  return t ? e : r2;
}
function L2(t, r2, e) {
  const n = (s) => s.pz, i = G(t.Fp, e.map(n));
  return e.map((s, f) => s.toAffine(i[f])).map(t.fromAffine);
}
function I2(t, r2) {
  if (!Number.isSafeInteger(t) || t <= 0 || t > r2)
    throw new Error("invalid window size, expected [1.." + r2 + "], got W=" + t);
}
function E2(t, r2) {
  I2(t, r2);
  const e = Math.ceil(r2 / t) + 1, n = 2 ** (t - 1), i = 2 ** t, o = N(t), s = BigInt(t);
  return { windows: e, windowSize: n, mask: o, maxNumber: i, shiftBy: s };
}
function v2(t, r2, e) {
  const { windowSize: n, mask: i, maxNumber: o, shiftBy: s } = e;
  let f = Number(t & i), a = t >> s;
  f > n && (f -= o, a += h4);
  const d2 = r2 * n, c3 = d2 + Math.abs(f) - 1, p = f === 0, l3 = f < 0, u = r2 % 2 !== 0;
  return { nextN: a, offset: c3, isZero: p, isNeg: l3, isNegF: u, offsetF: d2 };
}
function j2(t, r2) {
  if (!Array.isArray(t))
    throw new Error("array expected");
  t.forEach((e, n) => {
    if (!(e instanceof r2))
      throw new Error("invalid point at index " + n);
  });
}
function _4(t, r2) {
  if (!Array.isArray(t))
    throw new Error("array of scalars expected");
  t.forEach((e, n) => {
    if (!r2.isValid(e))
      throw new Error("invalid scalar at index " + n);
  });
}
var N3 = /* @__PURE__ */ new WeakMap();
var M3 = /* @__PURE__ */ new WeakMap();
function A2(t) {
  return M3.get(t) || 1;
}
function S3(t) {
  if (t !== g3)
    throw new Error("invalid wNAF");
}
function U4(t, r2) {
  return {
    constTimeNegate: b2,
    hasPrecomputes(e) {
      return A2(e) !== 1;
    },
    // non-const time multiplication ladder
    unsafeLadder(e, n, i = t.ZERO) {
      let o = e;
      for (; n > g3; )
        n & h4 && (i = i.add(o)), o = o.double(), n >>= h4;
      return i;
    },
    /**
     * Creates a wNAF precomputation window. Used for caching.
     * Default window size is set by `utils.precompute()` and is equal to 8.
     * Number of precomputed points depends on the curve size:
     * 2^(𝑊−1) * (Math.ceil(𝑛 / 𝑊) + 1), where:
     * - 𝑊 is the window size
     * - 𝑛 is the bitlength of the curve order.
     * For a 256-bit curve and window size 8, the number of precomputed points is 128 * 33 = 4224.
     * @param elm Point instance
     * @param W window size
     * @returns precomputed point tables flattened to a single array
     */
    precomputeWindow(e, n) {
      const { windows: i, windowSize: o } = E2(n, r2), s = [];
      let f = e, a = f;
      for (let d2 = 0; d2 < i; d2++) {
        a = f, s.push(a);
        for (let c3 = 1; c3 < o; c3++)
          a = a.add(f), s.push(a);
        f = a.double();
      }
      return s;
    },
    /**
     * Implements ec multiplication using precomputed tables and w-ary non-adjacent form.
     * @param W window size
     * @param precomputes precomputed tables
     * @param n scalar (we don't check here, but should be less than curve order)
     * @returns real and fake (for const-time) points
     */
    wNAF(e, n, i) {
      let o = t.ZERO, s = t.BASE;
      const f = E2(e, r2);
      for (let a = 0; a < f.windows; a++) {
        const { nextN: d2, offset: c3, isZero: p, isNeg: l3, isNegF: u, offsetF: m3 } = v2(i, a, f);
        i = d2, p ? s = s.add(b2(u, n[m3])) : o = o.add(b2(l3, n[c3]));
      }
      return S3(i), { p: o, f: s };
    },
    /**
     * Implements ec unsafe (non const-time) multiplication using precomputed tables and w-ary non-adjacent form.
     * @param W window size
     * @param precomputes precomputed tables
     * @param n scalar (we don't check here, but should be less than curve order)
     * @param acc accumulator point to add result of multiplication
     * @returns point
     */
    wNAFUnsafe(e, n, i, o = t.ZERO) {
      const s = E2(e, r2);
      for (let f = 0; f < s.windows && i !== g3; f++) {
        const { nextN: a, offset: d2, isZero: c3, isNeg: p } = v2(i, f, s);
        if (i = a, !c3) {
          const l3 = n[d2];
          o = o.add(p ? l3.negate() : l3);
        }
      }
      return S3(i), o;
    },
    getPrecomputes(e, n, i) {
      let o = N3.get(n);
      return o || (o = this.precomputeWindow(n, e), e !== 1 && (typeof i == "function" && (o = i(o)), N3.set(n, o))), o;
    },
    wNAFCached(e, n, i) {
      const o = A2(e);
      return this.wNAF(o, this.getPrecomputes(o, e, i), n);
    },
    wNAFCachedUnsafe(e, n, i, o) {
      const s = A2(e);
      return s === 1 ? this.unsafeLadder(e, n, o) : this.wNAFUnsafe(s, this.getPrecomputes(s, e, i), n, o);
    },
    // We calculate precomputes for elliptic curve point multiplication
    // using windowed method. This specifies window size and
    // stores precomputed values. Usually only base point would be precomputed.
    setWindowSize(e, n) {
      I2(n, r2), M3.set(e, n), N3.delete(e);
    }
  };
}
function $2(t, r2, e, n) {
  let i = r2, o = t.ZERO, s = t.ZERO;
  for (; e > g3 || n > g3; )
    e & h4 && (o = o.add(i)), n & h4 && (s = s.add(i)), i = i.double(), e >>= h4, n >>= h4;
  return { p1: o, p2: s };
}
function D3(t, r2, e, n) {
  j2(e, t), _4(n, r2);
  const i = e.length, o = n.length;
  if (i !== o)
    throw new Error("arrays of points and scalars must have equal length");
  const s = t.ZERO, f = M(BigInt(i));
  let a = 1;
  f > 12 ? a = f - 3 : f > 4 ? a = f - 2 : f > 0 && (a = 2);
  const d2 = N(a), c3 = new Array(Number(d2) + 1).fill(s), p = Math.floor((r2.BITS - 1) / a) * a;
  let l3 = s;
  for (let u = p; u >= 0; u -= a) {
    c3.fill(s);
    for (let w3 = 0; w3 < o; w3++) {
      const F3 = n[w3], y5 = Number(F3 >> BigInt(u) & d2);
      c3[y5] = c3[y5].add(e[w3]);
    }
    let m3 = s;
    for (let w3 = c3.length - 1, F3 = s; w3 > 0; w3--)
      F3 = F3.add(c3[w3]), m3 = m3.add(F3);
    if (l3 = l3.add(m3), u !== 0)
      for (let w3 = 0; w3 < a; w3++)
        l3 = l3.double();
  }
  return l3;
}
function x3(t, r2) {
  if (r2) {
    if (r2.ORDER !== t)
      throw new Error("Field.ORDER must match order: Fp == p, Fn == n");
    return P(r2), r2;
  } else
    return U3(t);
}
function G2(t, r2, e = {}) {
  if (!r2 || typeof r2 != "object")
    throw new Error(`expected valid ${t} CURVE object`);
  for (const f of ["p", "n", "h"]) {
    const a = r2[f];
    if (!(typeof a == "bigint" && a > g3))
      throw new Error(`CURVE.${f} must be positive bigint`);
  }
  const n = x3(r2.p, e.Fp), i = x3(r2.n, e.Fn), s = ["Gx", "Gy", "a", "b"];
  for (const f of s)
    if (!n.isValid(r2[f]))
      throw new Error(`CURVE.${f} must be valid field element of CURVE.Fp`);
  return { Fp: n, Fn: i };
}

// node_modules/@coinbase/cdp-hooks/dist/esm/index262.js
function st(e) {
  e.lowS !== void 0 && k2("lowS", e.lowS), e.prehash !== void 0 && k2("prehash", e.prehash);
}
var Ot = class extends Error {
  constructor(i = "") {
    super(i);
  }
};
var F2 = {
  // asn.1 DER encoding utils
  Err: Ot,
  // Basic building block is TLV (Tag-Length-Value)
  _tlv: {
    encode: (e, i) => {
      const { Err: t } = F2;
      if (e < 0 || e > 256)
        throw new t("tlv.encode: wrong tag");
      if (i.length & 1)
        throw new t("tlv.encode: unpadded data");
      const w3 = i.length / 2, p = j(w3);
      if (p.length / 2 & 128)
        throw new t("tlv.encode: long form length too big");
      const b3 = w3 > 127 ? j(p.length / 2 | 128) : "";
      return j(e) + b3 + p + i;
    },
    // v - value, l - left bytes (unparsed)
    decode(e, i) {
      const { Err: t } = F2;
      let w3 = 0;
      if (e < 0 || e > 256)
        throw new t("tlv.encode: wrong tag");
      if (i.length < 2 || i[w3++] !== e)
        throw new t("tlv.decode: wrong tlv");
      const p = i[w3++], b3 = !!(p & 128);
      let u = 0;
      if (!b3)
        u = p;
      else {
        const S4 = p & 127;
        if (!S4)
          throw new t("tlv.decode(long): indefinite length not supported");
        if (S4 > 4)
          throw new t("tlv.decode(long): byte length is too big");
        const N5 = i.subarray(w3, w3 + S4);
        if (N5.length !== S4)
          throw new t("tlv.decode: length bytes not complete");
        if (N5[0] === 0)
          throw new t("tlv.decode(long): zero leftmost byte");
        for (const q2 of N5)
          u = u << 8 | q2;
        if (w3 += S4, u < 128)
          throw new t("tlv.decode(long): not minimal encoding");
      }
      const x5 = i.subarray(w3, w3 + u);
      if (x5.length !== u)
        throw new t("tlv.decode: wrong value length");
      return { v: x5, l: i.subarray(w3 + u) };
    }
  },
  // https://crypto.stackexchange.com/a/57734 Leftmost bit of first byte is 'negative' flag,
  // since we always use positive integers here. It must always be empty:
  // - add zero byte if exists
  // - if next byte doesn't have a flag, leading zero is not allowed (minimal encoding)
  _int: {
    encode(e) {
      const { Err: i } = F2;
      if (e < U5)
        throw new i("integer: negative integers are not allowed");
      let t = j(e);
      if (Number.parseInt(t[0], 16) & 8 && (t = "00" + t), t.length & 1)
        throw new i("unexpected DER parsing assertion: unpadded hex");
      return t;
    },
    decode(e) {
      const { Err: i } = F2;
      if (e[0] & 128)
        throw new i("invalid signature integer: negative");
      if (e[0] === 0 && !(e[1] & 128))
        throw new i("invalid signature integer: unnecessary leading zero");
      return H2(e);
    }
  },
  toSig(e) {
    const { Err: i, _int: t, _tlv: w3 } = F2, p = _3("signature", e), { v: b3, l: u } = w3.decode(48, p);
    if (u.length)
      throw new i("invalid signature: left bytes after parsing");
    const { v: x5, l: S4 } = w3.decode(2, b3), { v: N5, l: q2 } = w3.decode(2, S4);
    if (q2.length)
      throw new i("invalid signature: left bytes after parsing");
    return { r: t.decode(x5), s: t.decode(N5) };
  },
  hexFromSig(e) {
    const { _tlv: i, _int: t } = F2, w3 = i.encode(2, t.encode(e.r)), p = i.encode(2, t.encode(e.s)), b3 = w3 + p;
    return i.encode(48, b3);
  }
};
var U5 = BigInt(0);
var k4 = BigInt(1);
var At = BigInt(2);
var W2 = BigInt(3);
var Zt = BigInt(4);
function Tt(e, i, t) {
  function w3(p) {
    const b3 = e.sqr(p), u = e.mul(b3, p);
    return e.add(e.add(u, e.mul(p, i)), t);
  }
  return w3;
}
function lt(e, i, t) {
  const { BYTES: w3 } = e;
  function p(b3) {
    let u;
    if (typeof b3 == "bigint")
      u = b3;
    else {
      let x5 = _3("private key", b3);
      if (i) {
        if (!i.includes(x5.length * 2))
          throw new Error("invalid private key");
        const S4 = new Uint8Array(w3);
        S4.set(x5, S4.length - x5.length), x5 = S4;
      }
      try {
        u = e.fromBytes(x5);
      } catch {
        throw new Error(`invalid private key: expected ui8a of size ${w3}, got ${typeof b3}`);
      }
    }
    if (t && (u = e.create(u)), !e.isValidNot0(u))
      throw new Error("invalid private key: out of range [1..N-1]");
    return u;
  }
  return p;
}
function zt(e, i = {}) {
  const { Fp: t, Fn: w3 } = G2("weierstrass", e, i), { h: p, n: b3 } = e;
  z(i, {}, {
    allowInfinityPoint: "boolean",
    clearCofactor: "function",
    isTorsionFree: "function",
    fromBytes: "function",
    toBytes: "function",
    endo: "object",
    wrapPrivateKey: "boolean"
  });
  const { endo: u } = i;
  if (u && (!t.is0(e.a) || typeof u.beta != "bigint" || typeof u.splitScalar != "function"))
    throw new Error('invalid endo: expected "beta": bigint and "splitScalar": function');
  function x5() {
    if (!t.isOdd)
      throw new Error("compression is not supported: Field does not have .isOdd()");
  }
  function S4(v3, r2, s) {
    const { x: n, y: o } = r2.toAffine(), a = t.toBytes(n);
    if (k2("isCompressed", s), s) {
      x5();
      const d2 = !t.isOdd(o);
      return R(ut(d2), a);
    } else
      return R(Uint8Array.of(4), a, t.toBytes(o));
  }
  function N5(v3) {
    c(v3);
    const r2 = t.BYTES, s = r2 + 1, n = 2 * r2 + 1, o = v3.length, a = v3[0], d2 = v3.subarray(1);
    if (o === s && (a === 2 || a === 3)) {
      const c3 = t.fromBytes(d2);
      if (!t.isValid(c3))
        throw new Error("bad point: is not on curve, wrong x");
      const l3 = T3(c3);
      let h5;
      try {
        h5 = t.sqrt(l3);
      } catch (g4) {
        const m3 = g4 instanceof Error ? ": " + g4.message : "";
        throw new Error("bad point: is not on curve, sqrt error" + m3);
      }
      x5();
      const f = t.isOdd(h5);
      return (a & 1) === 1 !== f && (h5 = t.neg(h5)), { x: c3, y: h5 };
    } else if (o === n && a === 4) {
      const c3 = t.fromBytes(d2.subarray(r2 * 0, r2 * 1)), l3 = t.fromBytes(d2.subarray(r2 * 1, r2 * 2));
      if (!L3(c3, l3))
        throw new Error("bad point: is not on curve");
      return { x: c3, y: l3 };
    } else
      throw new Error(`bad point: got length ${o}, expected compressed=${s} or uncompressed=${n}`);
  }
  const q2 = i.toBytes || S4, C = i.fromBytes || N5, T3 = Tt(t, e.a, e.b);
  function L3(v3, r2) {
    const s = t.sqr(r2), n = T3(v3);
    return t.eql(s, n);
  }
  if (!L3(e.Gx, e.Gy))
    throw new Error("bad curve params: generator point");
  const J2 = t.mul(t.pow(e.a, W2), Zt), tt = t.mul(t.sqr(e.b), BigInt(27));
  if (t.is0(t.add(J2, tt)))
    throw new Error("bad curve params: a or b");
  function K2(v3, r2, s = false) {
    if (!t.isValid(r2) || s && t.is0(r2))
      throw new Error(`bad point coordinate ${v3}`);
    return r2;
  }
  function G3(v3) {
    if (!(v3 instanceof y5))
      throw new Error("ProjectivePoint expected");
  }
  const X = D((v3, r2) => {
    const { px: s, py: n, pz: o } = v3;
    if (t.eql(o, t.ONE))
      return { x: s, y: n };
    const a = v3.is0();
    r2 == null && (r2 = a ? t.ONE : t.inv(o));
    const d2 = t.mul(s, r2), c3 = t.mul(n, r2), l3 = t.mul(o, r2);
    if (a)
      return { x: t.ZERO, y: t.ZERO };
    if (!t.eql(l3, t.ONE))
      throw new Error("invZ was invalid");
    return { x: d2, y: c3 };
  }), V4 = D((v3) => {
    if (v3.is0()) {
      if (i.allowInfinityPoint && !t.is0(v3.py))
        return;
      throw new Error("bad point: ZERO");
    }
    const { x: r2, y: s } = v3.toAffine();
    if (!t.isValid(r2) || !t.isValid(s))
      throw new Error("bad point: x or y not field elements");
    if (!L3(r2, s))
      throw new Error("bad point: equation left != right");
    if (!v3.isTorsionFree())
      throw new Error("bad point: not in prime-order subgroup");
    return true;
  });
  function $3(v3, r2, s, n, o) {
    return s = new y5(t.mul(s.px, v3), s.py, s.pz), r2 = b2(n, r2), s = b2(o, s), r2.add(s);
  }
  class y5 {
    /** Does NOT validate if the point is valid. Use `.assertValidity()`. */
    constructor(r2, s, n) {
      this.px = K2("x", r2), this.py = K2("y", s, true), this.pz = K2("z", n), Object.freeze(this);
    }
    /** Does NOT validate if the point is valid. Use `.assertValidity()`. */
    static fromAffine(r2) {
      const { x: s, y: n } = r2 || {};
      if (!r2 || !t.isValid(s) || !t.isValid(n))
        throw new Error("invalid affine point");
      if (r2 instanceof y5)
        throw new Error("projective point not allowed");
      return t.is0(s) && t.is0(n) ? y5.ZERO : new y5(s, n, t.ONE);
    }
    get x() {
      return this.toAffine().x;
    }
    get y() {
      return this.toAffine().y;
    }
    static normalizeZ(r2) {
      return L2(y5, "pz", r2);
    }
    static fromBytes(r2) {
      return c(r2), y5.fromHex(r2);
    }
    /** Converts hash string or Uint8Array to Point. */
    static fromHex(r2) {
      const s = y5.fromAffine(C(_3("pointHex", r2)));
      return s.assertValidity(), s;
    }
    /** Multiplies generator point by privateKey. */
    static fromPrivateKey(r2) {
      const s = lt(w3, i.allowedPrivateKeyLengths, i.wrapPrivateKey);
      return y5.BASE.multiply(s(r2));
    }
    /** Multiscalar Multiplication */
    static msm(r2, s) {
      return D3(y5, w3, r2, s);
    }
    /**
     *
     * @param windowSize
     * @param isLazy true will defer table computation until the first multiplication
     * @returns
     */
    precompute(r2 = 8, s = true) {
      return _5.setWindowSize(this, r2), s || this.multiply(W2), this;
    }
    /** "Private method", don't use it directly */
    _setWindowSize(r2) {
      this.precompute(r2);
    }
    // TODO: return `this`
    /** A point on curve is valid if it conforms to equation. */
    assertValidity() {
      V4(this);
    }
    hasEvenY() {
      const { y: r2 } = this.toAffine();
      if (!t.isOdd)
        throw new Error("Field doesn't support isOdd");
      return !t.isOdd(r2);
    }
    /** Compare one point to another. */
    equals(r2) {
      G3(r2);
      const { px: s, py: n, pz: o } = this, { px: a, py: d2, pz: c3 } = r2, l3 = t.eql(t.mul(s, c3), t.mul(a, o)), h5 = t.eql(t.mul(n, c3), t.mul(d2, o));
      return l3 && h5;
    }
    /** Flips point to one corresponding to (x, -y) in Affine coordinates. */
    negate() {
      return new y5(this.px, t.neg(this.py), this.pz);
    }
    // Renes-Costello-Batina exception-free doubling formula.
    // There is 30% faster Jacobian formula, but it is not complete.
    // https://eprint.iacr.org/2015/1060, algorithm 3
    // Cost: 8M + 3S + 3*a + 2*b3 + 15add.
    double() {
      const { a: r2, b: s } = e, n = t.mul(s, W2), { px: o, py: a, pz: d2 } = this;
      let c3 = t.ZERO, l3 = t.ZERO, h5 = t.ZERO, f = t.mul(o, o), B6 = t.mul(a, a), g4 = t.mul(d2, d2), m3 = t.mul(o, a);
      return m3 = t.add(m3, m3), h5 = t.mul(o, d2), h5 = t.add(h5, h5), c3 = t.mul(r2, h5), l3 = t.mul(n, g4), l3 = t.add(c3, l3), c3 = t.sub(B6, l3), l3 = t.add(B6, l3), l3 = t.mul(c3, l3), c3 = t.mul(m3, c3), h5 = t.mul(n, h5), g4 = t.mul(r2, g4), m3 = t.sub(f, g4), m3 = t.mul(r2, m3), m3 = t.add(m3, h5), h5 = t.add(f, f), f = t.add(h5, f), f = t.add(f, g4), f = t.mul(f, m3), l3 = t.add(l3, f), g4 = t.mul(a, d2), g4 = t.add(g4, g4), f = t.mul(g4, m3), c3 = t.sub(c3, f), h5 = t.mul(g4, B6), h5 = t.add(h5, h5), h5 = t.add(h5, h5), new y5(c3, l3, h5);
    }
    // Renes-Costello-Batina exception-free addition formula.
    // There is 30% faster Jacobian formula, but it is not complete.
    // https://eprint.iacr.org/2015/1060, algorithm 1
    // Cost: 12M + 0S + 3*a + 3*b3 + 23add.
    add(r2) {
      G3(r2);
      const { px: s, py: n, pz: o } = this, { px: a, py: d2, pz: c3 } = r2;
      let l3 = t.ZERO, h5 = t.ZERO, f = t.ZERO;
      const B6 = e.a, g4 = t.mul(e.b, W2);
      let m3 = t.mul(s, a), R3 = t.mul(n, d2), O4 = t.mul(o, c3), A3 = t.add(s, n), E3 = t.add(a, d2);
      A3 = t.mul(A3, E3), E3 = t.add(m3, R3), A3 = t.sub(A3, E3), E3 = t.add(s, o);
      let Z = t.add(a, c3);
      return E3 = t.mul(E3, Z), Z = t.add(m3, O4), E3 = t.sub(E3, Z), Z = t.add(n, o), l3 = t.add(d2, c3), Z = t.mul(Z, l3), l3 = t.add(R3, O4), Z = t.sub(Z, l3), f = t.mul(B6, E3), l3 = t.mul(g4, O4), f = t.add(l3, f), l3 = t.sub(R3, f), f = t.add(R3, f), h5 = t.mul(l3, f), R3 = t.add(m3, m3), R3 = t.add(R3, m3), O4 = t.mul(B6, O4), E3 = t.mul(g4, E3), R3 = t.add(R3, O4), O4 = t.sub(m3, O4), O4 = t.mul(B6, O4), E3 = t.add(E3, O4), m3 = t.mul(R3, E3), h5 = t.add(h5, m3), m3 = t.mul(Z, E3), l3 = t.mul(A3, l3), l3 = t.sub(l3, m3), m3 = t.mul(A3, R3), f = t.mul(Z, f), f = t.add(f, m3), new y5(l3, h5, f);
    }
    subtract(r2) {
      return this.add(r2.negate());
    }
    is0() {
      return this.equals(y5.ZERO);
    }
    /**
     * Constant time multiplication.
     * Uses wNAF method. Windowed method may be 10% faster,
     * but takes 2x longer to generate and consumes 2x memory.
     * Uses precomputes when available.
     * Uses endomorphism for Koblitz curves.
     * @param scalar by which the point would be multiplied
     * @returns New point
     */
    multiply(r2) {
      const { endo: s } = i;
      if (!w3.isValidNot0(r2))
        throw new Error("invalid scalar: out of range");
      let n, o;
      const a = (d2) => _5.wNAFCached(this, d2, y5.normalizeZ);
      if (s) {
        const { k1neg: d2, k1: c3, k2neg: l3, k2: h5 } = s.splitScalar(r2), { p: f, f: B6 } = a(c3), { p: g4, f: m3 } = a(h5);
        o = B6.add(m3), n = $3(s.beta, f, g4, d2, l3);
      } else {
        const { p: d2, f: c3 } = a(r2);
        n = d2, o = c3;
      }
      return y5.normalizeZ([n, o])[0];
    }
    /**
     * Non-constant-time multiplication. Uses double-and-add algorithm.
     * It's faster, but should only be used when you don't care about
     * an exposed private key e.g. sig verification, which works over *public* keys.
     */
    multiplyUnsafe(r2) {
      const { endo: s } = i, n = this;
      if (!w3.isValid(r2))
        throw new Error("invalid scalar: out of range");
      if (r2 === U5 || n.is0())
        return y5.ZERO;
      if (r2 === k4)
        return n;
      if (_5.hasPrecomputes(this))
        return this.multiply(r2);
      if (s) {
        const { k1neg: o, k1: a, k2neg: d2, k2: c3 } = s.splitScalar(r2), { p1: l3, p2: h5 } = $2(y5, n, a, c3);
        return $3(s.beta, l3, h5, o, d2);
      } else
        return _5.wNAFCachedUnsafe(n, r2);
    }
    multiplyAndAddUnsafe(r2, s, n) {
      const o = this.multiplyUnsafe(s).add(r2.multiplyUnsafe(n));
      return o.is0() ? void 0 : o;
    }
    /**
     * Converts Projective point to affine (x, y) coordinates.
     * @param invertedZ Z^-1 (inverted zero) - optional, precomputation is useful for invertBatch
     */
    toAffine(r2) {
      return X(this, r2);
    }
    /**
     * Checks whether Point is free of torsion elements (is in prime subgroup).
     * Always torsion-free for cofactor=1 curves.
     */
    isTorsionFree() {
      const { isTorsionFree: r2 } = i;
      return p === k4 ? true : r2 ? r2(y5, this) : _5.wNAFCachedUnsafe(this, b3).is0();
    }
    clearCofactor() {
      const { clearCofactor: r2 } = i;
      return p === k4 ? this : r2 ? r2(y5, this) : this.multiplyUnsafe(p);
    }
    toBytes(r2 = true) {
      return k2("isCompressed", r2), this.assertValidity(), q2(y5, this, r2);
    }
    /** @deprecated use `toBytes` */
    toRawBytes(r2 = true) {
      return this.toBytes(r2);
    }
    toHex(r2 = true) {
      return k(this.toBytes(r2));
    }
    toString() {
      return `<Point ${this.is0() ? "ZERO" : this.toHex()}>`;
    }
  }
  y5.BASE = new y5(e.Gx, e.Gy, t.ONE), y5.ZERO = new y5(t.ZERO, t.ONE, t.ZERO), y5.Fp = t, y5.Fn = w3;
  const P2 = w3.BITS, _5 = U4(y5, i.endo ? Math.ceil(P2 / 2) : P2);
  return y5;
}
function ut(e) {
  return Uint8Array.of(e ? 2 : 3);
}
function Nt(e, i, t = {}) {
  z(i, { hash: "function" }, {
    hmac: "function",
    lowS: "boolean",
    randomBytes: "function",
    bits2int: "function",
    bits2int_modN: "function"
  });
  const w3 = i.randomBytes || v, p = i.hmac || ((n, ...o) => y3(i.hash, n, R(...o))), { Fp: b3, Fn: u } = e, { ORDER: x5, BITS: S4 } = u;
  function N5(n) {
    const o = x5 >> k4;
    return n > o;
  }
  function q2(n) {
    return N5(n) ? u.neg(n) : n;
  }
  function C(n, o) {
    if (!u.isValidNot0(o))
      throw new Error(`invalid signature ${n}: out of range 1..CURVE.n`);
  }
  class T3 {
    constructor(o, a, d2) {
      C("r", o), C("s", a), this.r = o, this.s = a, d2 != null && (this.recovery = d2), Object.freeze(this);
    }
    // pair (bytes of r, bytes of s)
    static fromCompact(o) {
      const a = u.BYTES, d2 = _3("compactSignature", o, a * 2);
      return new T3(u.fromBytes(d2.subarray(0, a)), u.fromBytes(d2.subarray(a, a * 2)));
    }
    // DER encoded ECDSA signature
    // https://bitcoin.stackexchange.com/questions/57644/what-are-the-parts-of-a-bitcoin-transaction-input-script
    static fromDER(o) {
      const { r: a, s: d2 } = F2.toSig(_3("DER", o));
      return new T3(a, d2);
    }
    /**
     * @todo remove
     * @deprecated
     */
    assertValidity() {
    }
    addRecoveryBit(o) {
      return new T3(this.r, this.s, o);
    }
    // ProjPointType<bigint>
    recoverPublicKey(o) {
      const a = b3.ORDER, { r: d2, s: c3, recovery: l3 } = this;
      if (l3 == null || ![0, 1, 2, 3].includes(l3))
        throw new Error("recovery id invalid");
      if (x5 * At < a && l3 > 1)
        throw new Error("recovery id is ambiguous for h>1 curve");
      const f = l3 === 2 || l3 === 3 ? d2 + x5 : d2;
      if (!b3.isValid(f))
        throw new Error("recovery id 2 or 3 invalid");
      const B6 = b3.toBytes(f), g4 = e.fromHex(R(ut((l3 & 1) === 0), B6)), m3 = u.inv(f), R3 = V4(_3("msgHash", o)), O4 = u.create(-R3 * m3), A3 = u.create(c3 * m3), E3 = e.BASE.multiplyUnsafe(O4).add(g4.multiplyUnsafe(A3));
      if (E3.is0())
        throw new Error("point at infinify");
      return E3.assertValidity(), E3;
    }
    // Signatures should be low-s, to prevent malleability.
    hasHighS() {
      return N5(this.s);
    }
    normalizeS() {
      return this.hasHighS() ? new T3(this.r, u.neg(this.s), this.recovery) : this;
    }
    toBytes(o) {
      if (o === "compact")
        return R(u.toBytes(this.r), u.toBytes(this.s));
      if (o === "der")
        return I(F2.hexFromSig(this));
      throw new Error("invalid format");
    }
    // DER-encoded
    toDERRawBytes() {
      return this.toBytes("der");
    }
    toDERHex() {
      return k(this.toBytes("der"));
    }
    // padded bytes of r, then padded bytes of s
    toCompactRawBytes() {
      return this.toBytes("compact");
    }
    toCompactHex() {
      return k(this.toBytes("compact"));
    }
  }
  const L3 = lt(u, t.allowedPrivateKeyLengths, t.wrapPrivateKey), J2 = {
    isValidPrivateKey(n) {
      try {
        return L3(n), true;
      } catch {
        return false;
      }
    },
    normPrivateKeyToScalar: L3,
    /**
     * Produces cryptographically secure private key from random of size
     * (groupLen + ceil(groupLen / 2)) with modulo bias being negligible.
     */
    randomPrivateKey: () => {
      const n = x5;
      return D2(w3(W(n)), n);
    },
    precompute(n = 8, o = e.BASE) {
      return o.precompute(n, false);
    }
  };
  function tt(n, o = true) {
    return e.fromPrivateKey(n).toBytes(o);
  }
  function K2(n) {
    if (typeof n == "bigint")
      return false;
    if (n instanceof e)
      return true;
    const a = _3("key", n).length, d2 = b3.BYTES, c3 = d2 + 1, l3 = 2 * d2 + 1;
    if (!(t.allowedPrivateKeyLengths || u.BYTES === c3))
      return a === c3 || a === l3;
  }
  function G3(n, o, a = true) {
    if (K2(n) === true)
      throw new Error("first arg must be private key");
    if (K2(o) === false)
      throw new Error("second arg must be public key");
    return e.fromHex(o).multiply(L3(n)).toBytes(a);
  }
  const X = i.bits2int || function(n) {
    if (n.length > 8192)
      throw new Error("input is too large");
    const o = H2(n), a = n.length * 8 - S4;
    return a > 0 ? o >> BigInt(a) : o;
  }, V4 = i.bits2int_modN || function(n) {
    return u.create(X(n));
  }, $3 = N(S4);
  function y5(n) {
    return $("num < 2^" + S4, n, U5, $3), u.toBytes(n);
  }
  function P2(n, o, a = _5) {
    if (["recovered", "canonical"].some((A3) => A3 in a))
      throw new Error("sign() legacy options not supported");
    const { hash: d2 } = i;
    let { lowS: c3, prehash: l3, extraEntropy: h5 } = a;
    c3 == null && (c3 = true), n = _3("msgHash", n), st(a), l3 && (n = _3("prehashed msgHash", d2(n)));
    const f = V4(n), B6 = L3(o), g4 = [y5(B6), y5(f)];
    if (h5 != null && h5 !== false) {
      const A3 = h5 === true ? w3(b3.BYTES) : h5;
      g4.push(_3("extraEntropy", A3));
    }
    const m3 = R(...g4), R3 = f;
    function O4(A3) {
      const E3 = X(A3);
      if (!u.isValidNot0(E3))
        return;
      const Z = u.inv(E3), j3 = e.BASE.multiply(E3).toAffine(), Y2 = u.create(j3.x);
      if (Y2 === U5)
        return;
      const H4 = u.create(Z * u.create(R3 + Y2 * B6));
      if (H4 === U5)
        return;
      let et = (j3.x === Y2 ? 0 : 2) | Number(j3.y & k4), D4 = H4;
      return c3 && N5(H4) && (D4 = q2(H4), et ^= 1), new T3(Y2, D4, et);
    }
    return { seed: m3, k2sig: O4 };
  }
  const _5 = { lowS: i.lowS, prehash: false }, v3 = { lowS: i.lowS, prehash: false };
  function r2(n, o, a = _5) {
    const { seed: d2, k2sig: c3 } = P2(n, o, a);
    return R2(i.hash.outputLen, u.BYTES, p)(d2, c3);
  }
  e.BASE.precompute(8);
  function s(n, o, a, d2 = v3) {
    const c3 = n;
    o = _3("msgHash", o), a = _3("publicKey", a), st(d2);
    const { lowS: l3, prehash: h5, format: f } = d2;
    if ("strict" in d2)
      throw new Error("options.strict was renamed to lowS");
    if (f !== void 0 && !["compact", "der", "js"].includes(f))
      throw new Error('format must be "compact", "der" or "js"');
    const B6 = typeof c3 == "string" || w(c3), g4 = !B6 && !f && typeof c3 == "object" && c3 !== null && typeof c3.r == "bigint" && typeof c3.s == "bigint";
    if (!B6 && !g4)
      throw new Error("invalid signature, expected Uint8Array, hex string or Signature instance");
    let m3, R3;
    try {
      if (g4)
        if (f === void 0 || f === "js")
          m3 = new T3(c3.r, c3.s);
        else
          throw new Error("invalid format");
      if (B6) {
        try {
          f !== "compact" && (m3 = T3.fromDER(c3));
        } catch (D4) {
          if (!(D4 instanceof F2.Err))
            throw D4;
        }
        !m3 && f !== "der" && (m3 = T3.fromCompact(c3));
      }
      R3 = e.fromHex(a);
    } catch {
      return false;
    }
    if (!m3 || l3 && m3.hasHighS())
      return false;
    h5 && (o = i.hash(o));
    const { r: O4, s: A3 } = m3, E3 = V4(o), Z = u.inv(A3), j3 = u.create(E3 * Z), Y2 = u.create(O4 * Z), H4 = e.BASE.multiplyUnsafe(j3).add(R3.multiplyUnsafe(Y2));
    return H4.is0() ? false : u.create(H4.x) === O4;
  }
  return Object.freeze({
    getPublicKey: tt,
    getSharedSecret: G3,
    sign: r2,
    verify: s,
    utils: J2,
    Point: e,
    Signature: T3
  });
}
function Ft(e) {
  const i = {
    a: e.a,
    b: e.b,
    p: e.Fp.ORDER,
    n: e.n,
    h: e.h,
    Gx: e.Gx,
    Gy: e.Gy
  }, t = e.Fp, w3 = U3(i.n, e.nBitLength), p = {
    Fp: t,
    Fn: w3,
    allowedPrivateKeyLengths: e.allowedPrivateKeyLengths,
    allowInfinityPoint: e.allowInfinityPoint,
    endo: e.endo,
    wrapPrivateKey: e.wrapPrivateKey,
    isTorsionFree: e.isTorsionFree,
    clearCofactor: e.clearCofactor,
    fromBytes: e.fromBytes,
    toBytes: e.toBytes
  };
  return { CURVE: i, curveOpts: p };
}
function Lt(e) {
  const { CURVE: i, curveOpts: t } = Ft(e), w3 = {
    hash: e.hash,
    hmac: e.hmac,
    randomBytes: e.randomBytes,
    lowS: e.lowS,
    bits2int: e.bits2int,
    bits2int_modN: e.bits2int_modN
  };
  return { CURVE: i, curveOpts: t, ecdsaOpts: w3 };
}
function _t(e, i) {
  return Object.assign({}, i, {
    ProjectivePoint: i.Point,
    CURVE: e
  });
}
function It(e) {
  const { CURVE: i, curveOpts: t, ecdsaOpts: w3 } = Lt(e), p = zt(i, t), b3 = Nt(p, w3, t);
  return _t(e, b3);
}

// node_modules/@coinbase/cdp-hooks/dist/esm/index242.js
function c2(e, t) {
  const r2 = (o) => It({ ...e, hash: o });
  return { ...r2(t), create: r2 };
}

// node_modules/@coinbase/cdp-hooks/dist/esm/index161.js
var B5 = {
  p: BigInt("0xfffffffffffffffffffffffffffffffffffffffffffffffffffffffefffffc2f"),
  n: BigInt("0xfffffffffffffffffffffffffffffffebaaedce6af48a03bbfd25e8cd0364141"),
  h: BigInt(1),
  a: BigInt(0),
  b: BigInt(7),
  Gx: BigInt("0x79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798"),
  Gy: BigInt("0x483ada7726a3c4655da4fbfc0e1108a8fd17b448a68554199c47d08ffb10d4b8")
};
BigInt(0);
var M4 = BigInt(1);
var x4 = BigInt(2);
var k5 = (n, f) => (n + f / x4) / f;
function N4(n) {
  const f = B5.p, e = BigInt(3), a = BigInt(6), I3 = BigInt(11), b3 = BigInt(22), s = BigInt(23), i = BigInt(44), r2 = BigInt(88), c3 = n * n * n % f, o = c3 * c3 * n % f, d2 = O3(o, e, f) * o % f, g4 = O3(d2, e, f) * o % f, m3 = O3(g4, x4, f) * c3 % f, p = O3(m3, I3, f) * m3 % f, _5 = O3(p, b3, f) * p % f, u = O3(_5, i, f) * _5 % f, q2 = O3(u, r2, f) * u % f, E3 = O3(q2, i, f) * _5 % f, C = O3(E3, e, f) * o % f, F3 = O3(C, s, f) * p % f, S4 = O3(F3, a, f) * c3 % f, w3 = O3(S4, x4, f);
  if (!l2.eql(l2.sqr(w3), n))
    throw new Error("Cannot find square root");
  return w3;
}
var l2 = U3(B5.p, void 0, void 0, { sqrt: N4 });
var V3 = c2({
  ...B5,
  Fp: l2,
  lowS: true,
  // Allow only low-S signatures by default in sign() and verify()
  endo: {
    // Endomorphism, see above
    beta: BigInt("0x7ae96a2b657c07106e64479eac3434e99cf0497512f58995c1396c28719501ee"),
    splitScalar: (n) => {
      const f = B5.n, e = BigInt("0x3086d221a7d46bcde86c90e49284eb15"), a = -M4 * BigInt("0xe4437ed6010e88286f547fa90abfe4c3"), I3 = BigInt("0x114ca50f7a8e2f3f657c1108d9d44cfd8"), b3 = e, s = BigInt("0x100000000000000000000000000000000"), i = k5(b3 * n, f), r2 = k5(-a * n, f);
      let c3 = h3(n - i * e - r2 * I3, f), o = h3(-i * a - r2 * b3, f);
      const d2 = c3 > s, g4 = o > s;
      if (d2 && (c3 = f - c3), g4 && (o = f - o), c3 > s || o > s)
        throw new Error("splitScalar: Endomorphism failed, k=" + n);
      return { k1neg: d2, k1: c3, k2neg: g4, k2: o };
    }
  }
}, y2);

export {
  y2 as y,
  V3 as V
};
/*! Bundled license information:

@coinbase/cdp-hooks/dist/esm/index263.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@coinbase/cdp-hooks/dist/esm/index243.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@coinbase/cdp-hooks/dist/esm/index265.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@coinbase/cdp-hooks/dist/esm/index262.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@coinbase/cdp-hooks/dist/esm/index242.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@coinbase/cdp-hooks/dist/esm/index161.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)
*/
//# sourceMappingURL=chunk-IQDEPRAA.js.map
